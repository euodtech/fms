<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: only allow HTTPS connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Allow cleartext for localhost during development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <!-- Dev machine LAN IP for physical-device testing over Wi-Fi (DHCP lease; update when it drifts) -->
        <domain includeSubdomains="true">10.123.143.206</domain>
    </domain-config>
</network-security-config>
